The run struct represents a random realization of a stochastic process, and the
dynamic keeps track of the ecDNA dynamics over time (e.g. the mean of the ecDNA
distribution for the run over the iterations).
